export enum SOStatus {
  PENDING = 'pending',
  CONFIRMED = 'confirmed',
  ISSUED = 'issued',
  DELIVERED = 'delivered',
  CANCEL = 'cancel',
}

export enum UserRoles {
  ADMIN = 'admin',
  MANAGER = 'manager',
  STAFF = 'staff',
}

export enum InventoryStatus {
  IN_STOCK = 'in-stock',
  OUT_OF_STOCK = 'out-of-stock'
}

